<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || "Eventicus" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="<%= content_for(:description) || 'Discover events in your city' %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# RSS/iCal Autodiscovery %>
    <%= auto_discovery_link_tag :rss, feeds_events_url(format: :rss), title: "Eventicus - All Events (RSS)" %>
    <%= auto_discovery_link_tag :atom, feeds_ical_url(format: :ics), type: "text/calendar", title: "Eventicus - All Events (iCal)" %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <style>[x-cloak] { display: none !important; }</style>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 dark:bg-gray-900 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-white dark:bg-gray-800 shadow-sm border-b border-gray-200 dark:border-gray-700">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <!-- Logo & Main Nav -->
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center gap-2 text-xl font-bold text-indigo-600 dark:text-indigo-400" do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" viewBox="0 0 24 24" fill="currentColor">
                <path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V9h14v11zM9 11H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2zm-8 4H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2z"/>
              </svg>
              <span>eventicus</span>
            <% end %>

            <div class="hidden sm:ml-8 sm:flex sm:space-x-4">
              <%= link_to t("navigation.events"), events_path, class: "text-gray-700 dark:text-gray-200 hover:text-indigo-600 dark:hover:text-indigo-400 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to t("navigation.cities"), cities_path, class: "text-gray-700 dark:text-gray-200 hover:text-indigo-600 dark:hover:text-indigo-400 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to t("navigation.categories"), categories_path, class: "text-gray-700 dark:text-gray-200 hover:text-indigo-600 dark:hover:text-indigo-400 px-3 py-2 rounded-md text-sm font-medium" %>
            </div>
          </div>

          <!-- User Menu & Language -->
          <div class="flex items-center gap-4">
            <!-- Language Switcher -->
            <div class="flex items-center gap-1 text-sm">
              <%= link_to "DE", url_for(locale: :de), class: "#{I18n.locale == :de ? 'font-bold text-indigo-600' : 'text-gray-500 hover:text-gray-700'}" %>
              <span class="text-gray-300">|</span>
              <%= link_to "EN", url_for(locale: :en), class: "#{I18n.locale == :en ? 'font-bold text-indigo-600' : 'text-gray-500 hover:text-gray-700'}" %>
            </div>

            <% if user_signed_in? %>
              <div class="flex items-center gap-3">
                <%= link_to new_event_path, class: "bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition" do %>
                  + <%= t("events.index.add_event") %>
                <% end %>
                <%= link_to current_user.display_name, user_path(current_user.username), class: "text-gray-700 dark:text-gray-200 hover:text-indigo-600 text-sm font-medium" %>
                <%= button_to t("navigation.logout"), destroy_user_session_path, method: :delete, class: "text-gray-500 hover:text-gray-700 text-sm" %>
              </div>
            <% else %>
              <div class="flex items-center gap-3">
                <%= link_to t("navigation.login"), new_user_session_path, class: "text-gray-700 dark:text-gray-200 hover:text-indigo-600 text-sm font-medium" %>
                <%= link_to t("navigation.signup"), new_user_registration_path, class: "bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition" %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg" role="alert">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg" role="alert">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-white dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 mt-auto">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
          <p class="text-gray-500 text-sm">
            © <%= Date.current.year %> eventicus.de
          </p>
          <div class="flex items-center gap-4 text-sm text-gray-500">
            <%= link_to feeds_events_path(format: :rss), class: "hover:text-indigo-600 flex items-center gap-1", title: "RSS Feed" do %>
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                <path d="M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1z"/>
              </svg>
              RSS
            <% end %>
            <%= link_to feeds_ical_path(format: :ics), class: "hover:text-indigo-600 flex items-center gap-1", title: "iCal Feed" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
              </svg>
              iCal
            <% end %>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
